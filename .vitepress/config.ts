import { defineConfig } from 'vitepress'

export default defineConfig({
  lang: 'zh-CN',
  title: 'BsIndex',
  description: '不是的导航站',

  themeConfig: {
    socialLinks: [{ icon: 'github', link: 'https://github.com/bsdayo' }],
  },
})
